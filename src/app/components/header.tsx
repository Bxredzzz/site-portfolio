"use client";
import Menu from "./menu";
export default function Header() {
    return (
        <header className="fixedHeader justify-between items-center w-full p-8 bg  ">
            <Menu />
        </header>

    );
}
